// const Sequelize = require('sequelize')
// const pkg = require('../../package.json')

// // Grab the database name from package.json and automatically append '-test' if in a testing env.
// const databaseName = pkg.name + (process.env.NODE_ENV === 'test' ? '-test' : '')

// // Instantiate Sequelize server using process.env's db url OR database name and localhost; turn off logging
// const db = new Sequelize(process.env.DATABASE_URL || `postgres://localhost:5432/${databaseName}`, { logging: false })

// // Don't forget to export it!
// module.exports = db


// MOVED ALL OF THIS TO INDEX
